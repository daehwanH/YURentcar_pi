# MFRC522 사용을 위해 환경설정

# spi 활성화
sudo raspi-config

sudo reboot

lsmod | grep spi

# python2.7 설치

# 심볼릭 링크 파이썬3 에서 파이썬2로 변경
python --version

# pip 버전 변경

# 파이썬 기본 환경설정
sudo apt update
sudo apt upgrade
sudo apt-get install python-dev

# RPi.GPIO 모듈 설치



# RFID reader(MFRC522) 사용을 위해 모듈 복사
# SPI-Py git clone
sudo git clone http://github.com/lthiery/SPI-Py.git
cd SPI-Py/
sudo git checkout 8cce26b9ee6e69eb041e9d5665944b88688fca68
sudo python setup.py install

# MFRC522-python git clone
sudo git clone https://github.com/mxgxw/MFRC522-python.git
cd MFRC522-python/
python Read.py